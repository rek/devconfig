# Home mode: single external + laptop. Placement TBD — customize later.
# Switch via `mode home`. Loaded through ~/.config/hypr/modes/current.conf.

# Apps that should be running in home mode.
exec-once = uwsm-app -- vivaldi
exec-once = uwsm-app -- google-chrome-stable
